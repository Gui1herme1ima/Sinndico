# Fatia 4.7 — Central de notificações in-app — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sino no header com badge de não lidas, alimentado pelos mesmos 7 eventos que já disparam
push hoje (chat, comida×2, comunicado, reserva×2, assembleia, encomenda).

**Architecture:** Nova tabela `notificacoes` (uma linha por destinatário, não junção). Uma função
nova `notifyUsers()` em `notificationService.ts` grava a notificação in-app e dispara o push que já
existia — os 7 call sites nos controllers trocam `sendPushToTokens` por `notifyUsers`. Endpoints
REST simples (listar, contar não lidas, marcar lida, marcar todas) consumidos por um componente
`NotificationBell` no `Header`, com polling de 30s via `useQuery`.

**Tech Stack:** Express + node-pg-migrate + pg (backend); React + `@tanstack/react-query` +
react-router-dom + Tailwind (frontend). Sem framework de teste automatizado no backend nem no
frontend hoje — verificação é manual (rodar `npm run dev` dos dois apps + `curl`/UI), mesmo padrão
já usado nas fatias anteriores deste projeto.

## Global Constraints

- Documentação, commits e comunicação em português (spec: `CLAUDE.md`).
- Identidade visual obrigatória: usar tokens do design system (`--color-danger`, `--color-primary`,
  classes já usadas em `Badge`/`ThemeToggle`/`MoradorSelect`) — nunca hex direto.
- RLS por `condominio_id` em toda tabela nova, mesma policy usada em `device_tokens`
  (`docs/arquitetura.md`).
- `notifyUsers` nunca lança exceção — efeito colateral best-effort, mesma postura de
  `sendPushToTokens` (spec, seção Backend).
- Sem paginação além das últimas 20 notificações; sem websocket/SSE; sem preferências de
  notificação — tudo fora de escopo (spec, seção "Fora de escopo").
- Mapa `tipo → rota` fixo: `chat→/chat`, `comida→/comida`, `comunicado→/comunicados`,
  `reserva→/areas-comuns`, `assembleia→/assembleias`, `encomenda→/encomendas`.

---

### Task 1: Migration da tabela `notificacoes`

**Files:**
- Create: `apps/backend/src/database/migrations/1700000000026_create-notificacoes.ts`

**Interfaces:**
- Produces: tabela `notificacoes` com colunas `id, condominio_id, user_id, tipo, titulo, corpo,
  referencia_id, lida, created_at` — usada por todas as tasks seguintes.

- [ ] **Step 1: Criar o arquivo de migration**

```typescript
import { MigrationBuilder } from 'node-pg-migrate';

// Uma linha por (evento, destinatário) — não é junção estilo comunicado_leituras, porque cada
// notificação já nasce endereçada a um usuário só, igual às linhas de device_tokens/push.
export async function up(pgm: MigrationBuilder): Promise<void> {
  pgm.createTable('notificacoes', {
    id: { type: 'uuid', primaryKey: true, default: pgm.func('gen_random_uuid()') },
    condominio_id: { type: 'uuid', notNull: true, references: 'condominios', onDelete: 'CASCADE' },
    user_id: { type: 'uuid', notNull: true, references: 'users', onDelete: 'CASCADE' },
    tipo: {
      type: 'text',
      notNull: true,
      check: "tipo IN ('chat','comida','comunicado','reserva','assembleia','encomenda')",
    },
    titulo: { type: 'text', notNull: true },
    corpo: { type: 'text', notNull: true },
    referencia_id: { type: 'uuid', notNull: true },
    lida: { type: 'boolean', notNull: true, default: false },
    created_at: { type: 'timestamptz', notNull: true, default: pgm.func('now()') },
  });

  pgm.createIndex('notificacoes', 'condominio_id');
  pgm.createIndex('notificacoes', ['user_id', 'lida']);

  pgm.sql('ALTER TABLE notificacoes ENABLE ROW LEVEL SECURITY;');
  pgm.sql(`
    CREATE POLICY tenant_isolation ON notificacoes
    USING (condominio_id = NULLIF(current_setting('app.condominio_id', true), '')::uuid)
    WITH CHECK (condominio_id = NULLIF(current_setting('app.condominio_id', true), '')::uuid);
  `);
}

export async function down(pgm: MigrationBuilder): Promise<void> {
  pgm.dropTable('notificacoes');
}
```

- [ ] **Step 2: Rodar a migration e verificar**

Run: `cd apps/backend && npm run migrate`
Expected: log mostrando `1700000000026_create-notificacoes: migrating` seguido de `MIGRATIONS COMPLETE`, sem erro.

- [ ] **Step 3: Commit**

```bash
git add apps/backend/src/database/migrations/1700000000026_create-notificacoes.ts
git commit -m "feat(backend): migration da tabela notificacoes (fatia 4.7)"
```

---

### Task 2: Model `Notificacao.ts`

**Files:**
- Create: `apps/backend/src/models/Notificacao.ts`

**Interfaces:**
- Consumes: `TenantContext`, `withTenantContext` de `../database/tenantContext` (assinatura igual
  `DeviceToken.ts`: `withTenantContext(ctx, async (client) => {...})`).
- Produces: `NotificacaoTipo`, `Notificacao`, `CreateNotificacoesInput`, `createNotificacoes(ctx,
  userIds, input)`, `listNotificacoes(ctx, userId, apenasNaoLidas)`, `countNaoLidas(ctx, userId)`,
  `marcarComoLida(ctx, userId, id)`, `marcarTodasComoLidas(ctx, userId)` — usados por
  `notificationService.notifyUsers` (Task 3) e `notificacaoController` (Task 4).

- [ ] **Step 1: Criar o model**

```typescript
import { TenantContext, withTenantContext } from '../database/tenantContext';

export type NotificacaoTipo = 'chat' | 'comida' | 'comunicado' | 'reserva' | 'assembleia' | 'encomenda';

export interface Notificacao {
  id: string;
  condominio_id: string;
  user_id: string;
  tipo: NotificacaoTipo;
  titulo: string;
  corpo: string;
  referencia_id: string;
  lida: boolean;
  created_at: Date;
}

export interface CreateNotificacoesInput {
  tipo: NotificacaoTipo;
  titulo: string;
  corpo: string;
  referenciaId: string;
}

// Inserção em lote via unnest — uma linha por userId. Chamado só a partir de
// notificationService.notifyUsers, nunca direto pelos controllers.
export async function createNotificacoes(
  ctx: TenantContext,
  userIds: string[],
  input: CreateNotificacoesInput
): Promise<void> {
  if (userIds.length === 0) return;
  await withTenantContext(ctx, async (client) => {
    await client.query(
      `INSERT INTO notificacoes (condominio_id, user_id, tipo, titulo, corpo, referencia_id)
       SELECT $1, unnest($2::uuid[]), $3, $4, $5, $6`,
      [ctx.condominioId, userIds, input.tipo, input.titulo, input.corpo, input.referenciaId]
    );
  });
}

// Últimas 20 do usuário, mais recentes primeiro. apenasNaoLidas filtra lida = false.
export async function listNotificacoes(
  ctx: TenantContext,
  userId: string,
  apenasNaoLidas: boolean
): Promise<Notificacao[]> {
  return withTenantContext(ctx, async (client) => {
    const where = apenasNaoLidas ? 'AND lida = false' : '';
    const result = await client.query<Notificacao>(
      `SELECT * FROM notificacoes WHERE user_id = $1 ${where} ORDER BY created_at DESC LIMIT 20`,
      [userId]
    );
    return result.rows;
  });
}

export async function countNaoLidas(ctx: TenantContext, userId: string): Promise<number> {
  return withTenantContext(ctx, async (client) => {
    const result = await client.query<{ count: string }>(
      'SELECT COUNT(*) FROM notificacoes WHERE user_id = $1 AND lida = false',
      [userId]
    );
    return Number(result.rows[0].count);
  });
}

export async function marcarComoLida(ctx: TenantContext, userId: string, id: string): Promise<void> {
  await withTenantContext(ctx, async (client) => {
    await client.query('UPDATE notificacoes SET lida = true WHERE id = $1 AND user_id = $2', [id, userId]);
  });
}

export async function marcarTodasComoLidas(ctx: TenantContext, userId: string): Promise<void> {
  await withTenantContext(ctx, async (client) => {
    await client.query('UPDATE notificacoes SET lida = true WHERE user_id = $1 AND lida = false', [userId]);
  });
}
```

- [ ] **Step 2: Verificar compilação**

Run: `cd apps/backend && npx tsc -p tsconfig.json --noEmit`
Expected: sem erros de tipo.

- [ ] **Step 3: Commit**

```bash
git add apps/backend/src/models/Notificacao.ts
git commit -m "feat(backend): model Notificacao (fatia 4.7)"
```

---

### Task 3: `notifyUsers` em `notificationService.ts`

**Files:**
- Modify: `apps/backend/src/services/notificationService.ts`

**Interfaces:**
- Consumes: `createNotificacoes` de `../models/Notificacao` (Task 2), `listTokensForUsers` de
  `../models/DeviceToken` (já existe), `TenantContext` de `../database/tenantContext`.
- Produces: `notifyUsers(ctx: TenantContext, userIds: string[], input: { tipo: NotificacaoTipo,
  titulo: string, corpo: string, referenciaId: string }): Promise<void>` — usado pelas Tasks 5-10
  nos controllers.

- [ ] **Step 1: Adicionar `notifyUsers` ao final do arquivo**

```typescript
import { TenantContext } from '../database/tenantContext';
import { listTokensForUsers } from '../models/DeviceToken';
import { createNotificacoes, NotificacaoTipo } from '../models/Notificacao';
```

Adicionar esses 3 imports no topo do arquivo (junto ao `import admin from 'firebase-admin';`), e
ao final do arquivo:

```typescript
export interface NotifyUsersInput {
  tipo: NotificacaoTipo;
  titulo: string;
  corpo: string;
  referenciaId: string;
}

// Ponto único chamado pelos controllers pra qualquer evento que precise avisar usuários: grava a
// notificação in-app (uma linha por destinatário) e dispara o push pros mesmos usuários. Nunca
// lança — mesma postura best-effort do sendPushToTokens; grava a notificação antes de tentar o
// push porque é a parte mais importante (o push pode falhar silenciosamente sem prejuízo).
export async function notifyUsers(ctx: TenantContext, userIds: string[], input: NotifyUsersInput): Promise<void> {
  if (userIds.length === 0) return;

  try {
    await createNotificacoes(ctx, userIds, input);
  } catch (err) {
    console.error('[notificationService] erro ao gravar notificação in-app:', err);
  }

  const tokens = await listTokensForUsers(ctx, userIds);
  await sendPushToTokens(tokens, {
    title: input.titulo,
    body: input.corpo,
    data: { tipo: input.tipo, referenciaId: input.referenciaId },
  });
}
```

- [ ] **Step 2: Verificar compilação**

Run: `cd apps/backend && npx tsc -p tsconfig.json --noEmit`
Expected: sem erros de tipo.

- [ ] **Step 3: Commit**

```bash
git add apps/backend/src/services/notificationService.ts
git commit -m "feat(backend): notifyUsers grava notificacao in-app + dispara push (fatia 4.7)"
```

---

### Task 4: Endpoints REST de notificações

**Files:**
- Create: `apps/backend/src/controllers/notificacaoController.ts`
- Create: `apps/backend/src/routes/notificacoes.ts`
- Modify: `apps/backend/src/app.ts`

**Interfaces:**
- Consumes: `countNaoLidas, listNotificacoes, marcarComoLida, marcarTodasComoLidas, Notificacao`
  de `../models/Notificacao` (Task 2).
- Produces: rotas `GET /api/notificacoes`, `GET /api/notificacoes/nao-lidas/contagem`,
  `PATCH /api/notificacoes/:id/lida`, `PATCH /api/notificacoes/lidas` — consumidas pelo frontend
  (Task 11).

- [ ] **Step 1: Criar o controller**

```typescript
import { Request, Response } from 'express';
import { z } from 'zod';

import {
  countNaoLidas,
  listNotificacoes,
  marcarComoLida,
  marcarTodasComoLidas,
  Notificacao,
} from '../models/Notificacao';

function tenantContextOf(req: Request) {
  return { userId: req.user!.id, condominioId: req.user!.condominioId };
}

function toNotificacaoResponse(notificacao: Notificacao) {
  return {
    id: notificacao.id,
    tipo: notificacao.tipo,
    titulo: notificacao.titulo,
    corpo: notificacao.corpo,
    referenciaId: notificacao.referencia_id,
    lida: notificacao.lida,
    createdAt: notificacao.created_at,
  };
}

const listQuerySchema = z.object({
  apenasNaoLidas: z.coerce.boolean().optional(),
});

export async function list(req: Request, res: Response) {
  const query = listQuerySchema.parse(req.query);
  const notificacoes = await listNotificacoes(tenantContextOf(req), req.user!.id, query.apenasNaoLidas ?? false);
  res.json(notificacoes.map(toNotificacaoResponse));
}

export async function contagemNaoLidas(req: Request, res: Response) {
  const contagem = await countNaoLidas(tenantContextOf(req), req.user!.id);
  res.json({ contagem });
}

export async function marcarLida(req: Request, res: Response) {
  const id = z.string().uuid().parse(req.params.id);
  await marcarComoLida(tenantContextOf(req), req.user!.id, id);
  res.status(204).send();
}

export async function marcarTodasLidas(req: Request, res: Response) {
  await marcarTodasComoLidas(tenantContextOf(req), req.user!.id);
  res.status(204).send();
}
```

- [ ] **Step 2: Criar as rotas**

```typescript
import { Router } from 'express';

import { contagemNaoLidas, list, marcarLida, marcarTodasLidas } from '../controllers/notificacaoController';
import { asyncHandler } from '../middleware/asyncHandler';
import { authenticate } from '../middleware/auth';

const router = Router();

router.use(asyncHandler(authenticate));

router.get('/', asyncHandler(list));
router.get('/nao-lidas/contagem', asyncHandler(contagemNaoLidas));
router.patch('/:id/lida', asyncHandler(marcarLida));
router.patch('/lidas', asyncHandler(marcarTodasLidas));

export default router;
```

Sem `authorize(...)` — qualquer role logada acessa (cada um só vê as próprias, filtrado por
`user_id` no model), mesmo espírito de `deviceTokens.ts`.

- [ ] **Step 3: Registrar no `app.ts`**

Em `apps/backend/src/app.ts`, adicionar o import junto aos demais (ordem alfabética, entre
`encomendasRoutes` e `reservasRoutes`):

```typescript
import notificacoesRoutes from './routes/notificacoes';
```

E o `app.use` junto aos demais:

```typescript
app.use('/api/notificacoes', notificacoesRoutes);
```

- [ ] **Step 4: Verificar compilação e testar manualmente**

Run: `cd apps/backend && npx tsc -p tsconfig.json --noEmit`
Expected: sem erros de tipo.

Run: `cd apps/backend && npm run dev` (em um terminal separado, deixar rodando)

Com o servidor no ar, pegar um token válido (login de teste — ver `docs/configuracao.md`) e rodar:

```bash
curl -s http://localhost:3000/api/notificacoes -H "Authorization: Bearer $TOKEN"
curl -s http://localhost:3000/api/notificacoes/nao-lidas/contagem -H "Authorization: Bearer $TOKEN"
```

Expected: `[]` na primeira (ainda não existe notificação nenhuma) e `{"contagem":0}` na segunda.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/controllers/notificacaoController.ts apps/backend/src/routes/notificacoes.ts apps/backend/src/app.ts
git commit -m "feat(backend): endpoints REST de notificacoes (fatia 4.7)"
```

---

### Task 5: Wire `notifyUsers` no chat

**Files:**
- Modify: `apps/backend/src/controllers/chatController.ts`

**Interfaces:**
- Consumes: `notifyUsers` de `../services/notificationService` (Task 3).

- [ ] **Step 1: Trocar os imports**

Em `chatController.ts`, substituir:

```typescript
import { listTokensForUsers } from '../models/DeviceToken';
import { findUserByIdForTenant, listAdminIdsForTenant } from '../models/User';
import { sendPushToTokens } from '../services/notificationService';
```

por:

```typescript
import { findUserByIdForTenant, listAdminIdsForTenant } from '../models/User';
import { notifyUsers } from '../services/notificationService';
```

- [ ] **Step 2: Trocar a chamada em `create`**

Substituir (linhas 67-73):

```typescript
  const destinatarios = req.user!.role === 'morador' ? await listAdminIdsForTenant(ctx) : [moradorId];
  const tokens = await listTokensForUsers(ctx, destinatarios);
  await sendPushToTokens(tokens, {
    title: req.user!.role === 'morador' ? 'Nova mensagem de morador' : 'Nova mensagem da administração',
    body: input.mensagem,
    data: { tipo: 'chat', moradorId },
  });
```

por:

```typescript
  const destinatarios = req.user!.role === 'morador' ? await listAdminIdsForTenant(ctx) : [moradorId];
  await notifyUsers(ctx, destinatarios, {
    tipo: 'chat',
    titulo: req.user!.role === 'morador' ? 'Nova mensagem de morador' : 'Nova mensagem da administração',
    corpo: input.mensagem,
    referenciaId: moradorId,
  });
```

- [ ] **Step 3: Verificar compilação**

Run: `cd apps/backend && npx tsc -p tsconfig.json --noEmit`
Expected: sem erros de tipo.

- [ ] **Step 4: Testar manualmente**

Com o backend e o frontend web rodando (`npm run dev` nos dois apps), logar como morador, enviar
uma mensagem em `/chat`, depois logar como admin do mesmo condomínio e rodar:

```bash
curl -s http://localhost:3000/api/notificacoes -H "Authorization: Bearer $TOKEN_ADMIN"
```

Expected: array com 1 item, `tipo: "chat"`, `titulo: "Nova mensagem de morador"`.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/controllers/chatController.ts
git commit -m "feat(backend): notificacao in-app no chat (fatia 4.7)"
```

---

### Task 6: Wire `notifyUsers` na comida

**Files:**
- Modify: `apps/backend/src/controllers/comidaController.ts`

**Interfaces:**
- Consumes: `notifyUsers` de `../services/notificationService` (Task 3).

- [ ] **Step 1: Trocar os imports**

Substituir:

```typescript
import { findUserByIdForTenant, listPorteiroIdsForTenant } from '../models/User';
import { listTokensForUsers } from '../models/DeviceToken';
import { ApiError } from '../middleware/errorHandler';
import { sendPushToTokens } from '../services/notificationService';
```

por:

```typescript
import { findUserByIdForTenant, listPorteiroIdsForTenant } from '../models/User';
import { ApiError } from '../middleware/errorHandler';
import { notifyUsers } from '../services/notificationService';
```

- [ ] **Step 2: Trocar a chamada em `create`**

Substituir (linhas 81-87):

```typescript
  const porteiroIds = await listPorteiroIdsForTenant(ctx);
  const tokens = await listTokensForUsers(ctx, porteiroIds);
  await sendPushToTokens(tokens, {
    title: 'Novo pedido de comida',
    body: `Pedido de ${comida.restaurante} a caminho.`,
    data: { tipo: 'comida', comidaId: comida.id },
  });
```

por:

```typescript
  const porteiroIds = await listPorteiroIdsForTenant(ctx);
  await notifyUsers(ctx, porteiroIds, {
    tipo: 'comida',
    titulo: 'Novo pedido de comida',
    corpo: `Pedido de ${comida.restaurante} a caminho.`,
    referenciaId: comida.id,
  });
```

- [ ] **Step 3: Trocar a chamada em `updateStatus`**

Substituir (linhas 153-160):

```typescript
  if (input.status === 'chegou') {
    const tokens = await listTokensForUsers(ctx, [comida.morador_id]);
    await sendPushToTokens(tokens, {
      title: 'Seu pedido chegou',
      body: `${comida.restaurante} chegou na portaria.`,
      data: { tipo: 'comida', comidaId: comida.id },
    });
  }
```

por:

```typescript
  if (input.status === 'chegou') {
    await notifyUsers(ctx, [comida.morador_id], {
      tipo: 'comida',
      titulo: 'Seu pedido chegou',
      corpo: `${comida.restaurante} chegou na portaria.`,
      referenciaId: comida.id,
    });
  }
```

- [ ] **Step 4: Verificar compilação**

Run: `cd apps/backend && npx tsc -p tsconfig.json --noEmit`
Expected: sem erros de tipo.

- [ ] **Step 5: Testar manualmente**

Logar como morador, criar um pedido em `/comida`. Logar como porteiro do mesmo condomínio e
conferir `GET /api/notificacoes` — item `tipo: "comida"`, `titulo: "Novo pedido de comida"`. Depois,
como porteiro, marcar status `chegou`; conferir como o morador que a notificação apareceu com
`titulo: "Seu pedido chegou"`.

- [ ] **Step 6: Commit**

```bash
git add apps/backend/src/controllers/comidaController.ts
git commit -m "feat(backend): notificacao in-app na comida (fatia 4.7)"
```

---

### Task 7: Wire `notifyUsers` no comunicado

**Files:**
- Modify: `apps/backend/src/controllers/comunicadoController.ts`
- Modify: `apps/backend/src/models/DeviceToken.ts`

**Interfaces:**
- Consumes: `notifyUsers` de `../services/notificationService` (Task 3), `listUsersForTenant` de
  `../models/User` (já existe).

- [ ] **Step 1: Trocar os imports**

Substituir:

```typescript
import { listTokensForCondominio } from '../models/DeviceToken';
import { sendPushToTokens } from '../services/notificationService';
```

por:

```typescript
import { listUsersForTenant } from '../models/User';
import { notifyUsers } from '../services/notificationService';
```

- [ ] **Step 2: Trocar a chamada em `create`**

Substituir (linhas 46-51):

```typescript
  const tokens = await listTokensForCondominio(ctx);
  await sendPushToTokens(tokens, {
    title: 'Novo comunicado',
    body: input.titulo,
    data: { tipo: 'comunicado', comunicadoId: comunicado.id },
  });
```

por:

```typescript
  const destinatarios = (await listUsersForTenant(ctx)).map((u) => u.id);
  await notifyUsers(ctx, destinatarios, {
    tipo: 'comunicado',
    titulo: 'Novo comunicado',
    corpo: input.titulo,
    referenciaId: comunicado.id,
  });
```

- [ ] **Step 3: Remover `listTokensForCondominio`, agora sem uso**

Em `apps/backend/src/models/DeviceToken.ts`, remover a função (era usada só pelo comunicado):

```typescript
// Usado pelo gatilho de "novo comunicado" - todo mundo do condomínio (RLS já restringe ao tenant).
export async function listTokensForCondominio(ctx: TenantContext): Promise<string[]> {
  return withTenantContext(ctx, async (client) => {
    const result = await client.query<{ token: string }>('SELECT token FROM device_tokens');
    return result.rows.map((r) => r.token);
  });
}
```

- [ ] **Step 4: Verificar compilação**

Run: `cd apps/backend && npx tsc -p tsconfig.json --noEmit`
Expected: sem erros de tipo.

- [ ] **Step 5: Testar manualmente**

Logar como admin, criar um comunicado em `/comunicados`. Logar como morador e porteiro do mesmo
condomínio (contas diferentes) e conferir `GET /api/notificacoes` pros dois — ambos devem ter
recebido, `tipo: "comunicado"`.

- [ ] **Step 6: Commit**

```bash
git add apps/backend/src/controllers/comunicadoController.ts apps/backend/src/models/DeviceToken.ts
git commit -m "feat(backend): notificacao in-app no comunicado (fatia 4.7)"
```

---

### Task 8: Wire `notifyUsers` na reserva

**Files:**
- Modify: `apps/backend/src/controllers/reservaController.ts`

**Interfaces:**
- Consumes: `notifyUsers` de `../services/notificationService` (Task 3).

- [ ] **Step 1: Trocar os imports**

Substituir:

```typescript
import { listTokensForUsers } from '../models/DeviceToken';
import {
  createReserva,
  findReservaById,
  hasConflictingReserva,
  listReservas,
  Reserva,
  updateReservaStatus,
} from '../models/Reserva';
import { listAdminIdsForTenant } from '../models/User';
import { sendPushToTokens } from '../services/notificationService';
```

por:

```typescript
import {
  createReserva,
  findReservaById,
  hasConflictingReserva,
  listReservas,
  Reserva,
  updateReservaStatus,
} from '../models/Reserva';
import { listAdminIdsForTenant } from '../models/User';
import { notifyUsers } from '../services/notificationService';
```

- [ ] **Step 2: Trocar a chamada em `create`**

Substituir (linhas 86-92):

```typescript
  const adminIds = await listAdminIdsForTenant(ctx);
  const tokens = await listTokensForUsers(ctx, adminIds);
  await sendPushToTokens(tokens, {
    title: 'Nova reserva pendente',
    body: `${area.nome} — aguardando aprovação.`,
    data: { tipo: 'reserva', reservaId: reserva.id },
  });
```

por:

```typescript
  const adminIds = await listAdminIdsForTenant(ctx);
  await notifyUsers(ctx, adminIds, {
    tipo: 'reserva',
    titulo: 'Nova reserva pendente',
    corpo: `${area.nome} — aguardando aprovação.`,
    referenciaId: reserva.id,
  });
```

- [ ] **Step 3: Trocar a chamada em `updateStatus`**

Substituir (linhas 149-157):

```typescript
  if (!isMorador) {
    const tokens = await listTokensForUsers(ctx, [reserva.morador_id]);
    const title = input.status === 'aprovada' ? 'Reserva aprovada' : 'Reserva cancelada';
    await sendPushToTokens(tokens, {
      title,
      body: `Sua reserva foi ${input.status === 'aprovada' ? 'aprovada' : 'cancelada'} pelo síndico.`,
      data: { tipo: 'reserva', reservaId: reserva.id },
    });
  }
```

por:

```typescript
  if (!isMorador) {
    const titulo = input.status === 'aprovada' ? 'Reserva aprovada' : 'Reserva cancelada';
    await notifyUsers(ctx, [reserva.morador_id], {
      tipo: 'reserva',
      titulo,
      corpo: `Sua reserva foi ${input.status === 'aprovada' ? 'aprovada' : 'cancelada'} pelo síndico.`,
      referenciaId: reserva.id,
    });
  }
```

- [ ] **Step 4: Verificar compilação**

Run: `cd apps/backend && npx tsc -p tsconfig.json --noEmit`
Expected: sem erros de tipo.

- [ ] **Step 5: Testar manualmente**

Logar como morador, criar uma reserva em `/areas-comuns`. Logar como admin e conferir
`GET /api/notificacoes` — `tipo: "reserva"`, `titulo: "Nova reserva pendente"`. Aprovar a reserva
como admin; conferir como o morador que chegou `titulo: "Reserva aprovada"`.

- [ ] **Step 6: Commit**

```bash
git add apps/backend/src/controllers/reservaController.ts
git commit -m "feat(backend): notificacao in-app na reserva (fatia 4.7)"
```

---

### Task 9: Wire `notifyUsers` na assembleia

**Files:**
- Modify: `apps/backend/src/controllers/assembleiaController.ts`

**Interfaces:**
- Consumes: `notifyUsers` de `../services/notificationService` (Task 3).

- [ ] **Step 1: Trocar os imports**

Substituir:

```typescript
import { listTokensForUsers } from '../models/DeviceToken';
import { listUsersForTenant } from '../models/User';
import {
  ContagemVotos,
  contarVotos,
  contarVotosPorAssembleias,
  createVoto,
  findVotoDoMorador,
  listVotosDoMorador,
  OpcaoVoto,
} from '../models/Voto';
import { ApiError } from '../middleware/errorHandler';
import { sendPushToTokens } from '../services/notificationService';
```

por:

```typescript
import { listUsersForTenant } from '../models/User';
import {
  ContagemVotos,
  contarVotos,
  contarVotosPorAssembleias,
  createVoto,
  findVotoDoMorador,
  listVotosDoMorador,
  OpcaoVoto,
} from '../models/Voto';
import { ApiError } from '../middleware/errorHandler';
import { notifyUsers } from '../services/notificationService';
```

- [ ] **Step 2: Trocar `notificarMoradores`**

Substituir (linhas 64-69):

```typescript
async function notificarMoradores(req: Request, title: string, body: string, assembleiaId: string) {
  const ctx = tenantContextOf(req);
  const moradores = await listUsersForTenant(ctx, ['morador']);
  const tokens = await listTokensForUsers(ctx, moradores.map((m) => m.id));
  await sendPushToTokens(tokens, { title, body, data: { tipo: 'assembleia', assembleiaId } });
}
```

por:

```typescript
async function notificarMoradores(req: Request, titulo: string, corpo: string, assembleiaId: string) {
  const ctx = tenantContextOf(req);
  const moradores = await listUsersForTenant(ctx, ['morador']);
  await notifyUsers(ctx, moradores.map((m) => m.id), {
    tipo: 'assembleia',
    titulo,
    corpo,
    referenciaId: assembleiaId,
  });
}
```

- [ ] **Step 3: Verificar compilação**

Run: `cd apps/backend && npx tsc -p tsconfig.json --noEmit`
Expected: sem erros de tipo.

- [ ] **Step 4: Testar manualmente**

Logar como admin, criar uma assembleia em `/assembleias`. Logar como morador e conferir
`GET /api/notificacoes` — `tipo: "assembleia"`, `titulo: "Nova assembleia convocada"`. Abrir a
votação como admin; conferir que chegou `titulo: "Assembleia aberta para votação"`.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/controllers/assembleiaController.ts
git commit -m "feat(backend): notificacao in-app na assembleia (fatia 4.7)"
```

---

### Task 10: Wire `notifyUsers` na encomenda

**Files:**
- Modify: `apps/backend/src/controllers/encomendaController.ts`

**Interfaces:**
- Consumes: `notifyUsers` de `../services/notificationService` (Task 3).

- [ ] **Step 1: Trocar os imports**

Substituir:

```typescript
import { findUserByIdForTenant } from '../models/User';
import { listTokensForUsers } from '../models/DeviceToken';
import { sendPushToTokens } from '../services/notificationService';
```

por:

```typescript
import { findUserByIdForTenant } from '../models/User';
import { notifyUsers } from '../services/notificationService';
```

- [ ] **Step 2: Trocar a chamada em `create`**

Substituir (linhas 75-80):

```typescript
  const tokens = await listTokensForUsers(ctx, [encomenda.morador_id]);
  await sendPushToTokens(tokens, {
    title: 'Encomenda chegou',
    body: encomenda.descricao ?? 'Você tem uma nova encomenda na portaria.',
    data: { tipo: 'encomenda', encomendaId: encomenda.id },
  });
```

por:

```typescript
  await notifyUsers(ctx, [encomenda.morador_id], {
    tipo: 'encomenda',
    titulo: 'Encomenda chegou',
    corpo: encomenda.descricao ?? 'Você tem uma nova encomenda na portaria.',
    referenciaId: encomenda.id,
  });
```

- [ ] **Step 3: Verificar compilação**

Run: `cd apps/backend && npx tsc -p tsconfig.json --noEmit`
Expected: sem erros de tipo.

- [ ] **Step 4: Testar manualmente**

Logar como porteiro, registrar uma encomenda em `/encomendas` pra um morador. Logar como esse
morador e conferir `GET /api/notificacoes` — `tipo: "encomenda"`, `titulo: "Encomenda chegou"`.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/controllers/encomendaController.ts
git commit -m "feat(backend): notificacao in-app na encomenda (fatia 4.7)"
```

---

### Task 11: Tipos e client de API no frontend

**Files:**
- Modify: `apps/web/src/services/api/types.ts`
- Create: `apps/web/src/services/api/notificacoesApi.ts`

**Interfaces:**
- Consumes: `apiFetch` de `@/services/api/client` (já existe).
- Produces: `NotificacaoTipo`, `NotificacaoResponse` (types.ts); `notificacoesApi.list(apenasNaoLidas?)`,
  `.contagemNaoLidas()`, `.marcarLida(id)`, `.marcarTodasLidas()` — usados por `NotificationBell`
  (Task 13).

- [ ] **Step 1: Adicionar os tipos ao final de `types.ts`**

```typescript
export type NotificacaoTipo = 'chat' | 'comida' | 'comunicado' | 'reserva' | 'assembleia' | 'encomenda';

export interface NotificacaoResponse {
  id: string;
  tipo: NotificacaoTipo;
  titulo: string;
  corpo: string;
  referenciaId: string;
  lida: boolean;
  createdAt: string;
}
```

- [ ] **Step 2: Criar `notificacoesApi.ts`**

```typescript
import { apiFetch } from '@/services/api/client';
import type { NotificacaoResponse } from '@/services/api/types';

export const notificacoesApi = {
  list(apenasNaoLidas?: boolean): Promise<NotificacaoResponse[]> {
    const query = apenasNaoLidas ? '?apenasNaoLidas=true' : '';
    return apiFetch<NotificacaoResponse[]>(`/api/notificacoes${query}`);
  },

  contagemNaoLidas(): Promise<{ contagem: number }> {
    return apiFetch<{ contagem: number }>('/api/notificacoes/nao-lidas/contagem');
  },

  marcarLida(id: string): Promise<void> {
    return apiFetch<void>(`/api/notificacoes/${id}/lida`, { method: 'PATCH' });
  },

  marcarTodasLidas(): Promise<void> {
    return apiFetch<void>('/api/notificacoes/lidas', { method: 'PATCH' });
  },
};
```

- [ ] **Step 3: Verificar compilação**

Run: `cd apps/web && npm run typecheck`
Expected: sem erros de tipo.

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/services/api/types.ts apps/web/src/services/api/notificacoesApi.ts
git commit -m "feat(web): tipos e client de API de notificacoes (fatia 4.7)"
```

---

### Task 12: Helper de tempo relativo

**Files:**
- Create: `apps/web/src/lib/formatRelativeTime.ts`

**Interfaces:**
- Produces: `formatRelativeTime(iso: string): string` — usado por `NotificationBell` (Task 13).

- [ ] **Step 1: Criar o helper**

```typescript
const UNIDADES: Array<{ segundos: number; sufixo: string }> = [
  { segundos: 60, sufixo: 'agora' },
  { segundos: 3600, sufixo: 'min' },
  { segundos: 86400, sufixo: 'h' },
  { segundos: 2592000, sufixo: 'd' },
];

// Formato curto ("5 min", "3 h", "2 d") pro dropdown de notificações — sem dependência externa,
// só o suficiente pra esse uso (não é um formatador genérico de datas).
export function formatRelativeTime(iso: string): string {
  const diffSegundos = Math.max(0, (Date.now() - new Date(iso).getTime()) / 1000);

  if (diffSegundos < UNIDADES[0].segundos) return 'agora';
  if (diffSegundos < UNIDADES[1].segundos) return `${Math.floor(diffSegundos / 60)} min`;
  if (diffSegundos < UNIDADES[2].segundos) return `${Math.floor(diffSegundos / 3600)} h`;
  if (diffSegundos < UNIDADES[3].segundos) return `${Math.floor(diffSegundos / 86400)} d`;

  return new Intl.DateTimeFormat('pt-BR', { day: '2-digit', month: '2-digit' }).format(new Date(iso));
}
```

- [ ] **Step 2: Verificar compilação**

Run: `cd apps/web && npm run typecheck`
Expected: sem erros de tipo.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/lib/formatRelativeTime.ts
git commit -m "feat(web): helper formatRelativeTime (fatia 4.7)"
```

---

### Task 13: Componente `NotificationBell` + wiring no `Header`

**Files:**
- Create: `apps/web/src/components/Layout/NotificationBell.tsx`
- Modify: `apps/web/src/components/Layout/Header.tsx`

**Interfaces:**
- Consumes: `notificacoesApi` (Task 11), `formatRelativeTime` (Task 12), `NotificacaoResponse`,
  `NotificacaoTipo` (Task 11), `NotificacaoIcon` de `@/components/ui/icons` (já existe), `cn` de
  `@/lib/cn` (já existe).

- [ ] **Step 1: Criar `NotificationBell.tsx`**

```typescript
import { useEffect, useRef, useState } from 'react';
import { useQuery, useQueryClient } from '@tanstack/react-query';
import { useNavigate } from 'react-router-dom';

import { NotificacaoIcon } from '@/components/ui/icons';
import { formatRelativeTime } from '@/lib/formatRelativeTime';
import { cn } from '@/lib/cn';
import { notificacoesApi } from '@/services/api/notificacoesApi';
import type { NotificacaoResponse, NotificacaoTipo } from '@/services/api/types';

const ROTA_POR_TIPO: Record<NotificacaoTipo, string> = {
  chat: '/chat',
  comida: '/comida',
  comunicado: '/comunicados',
  reserva: '/areas-comuns',
  assembleia: '/assembleias',
  encomenda: '/encomendas',
};

export function NotificationBell() {
  const containerRef = useRef<HTMLDivElement>(null);
  const [open, setOpen] = useState(false);
  const navigate = useNavigate();
  const queryClient = useQueryClient();

  const contagemQuery = useQuery({
    queryKey: ['notificacoes-contagem'],
    queryFn: () => notificacoesApi.contagemNaoLidas(),
    refetchInterval: 30000,
  });

  const listaQuery = useQuery({
    queryKey: ['notificacoes-lista'],
    queryFn: () => notificacoesApi.list(),
    enabled: open,
  });

  const contagem = contagemQuery.data?.contagem ?? 0;

  useEffect(() => {
    function handleClickOutside(event: MouseEvent) {
      if (containerRef.current && !containerRef.current.contains(event.target as Node)) {
        setOpen(false);
      }
    }
    function handleEscape(event: KeyboardEvent) {
      if (event.key === 'Escape') setOpen(false);
    }
    document.addEventListener('mousedown', handleClickOutside);
    document.addEventListener('keydown', handleEscape);
    return () => {
      document.removeEventListener('mousedown', handleClickOutside);
      document.removeEventListener('keydown', handleEscape);
    };
  }, []);

  async function handleItemClick(notificacao: NotificacaoResponse) {
    setOpen(false);
    navigate(ROTA_POR_TIPO[notificacao.tipo]);
    if (!notificacao.lida) {
      queryClient.setQueryData<{ contagem: number }>(['notificacoes-contagem'], (old) => ({
        contagem: Math.max(0, (old?.contagem ?? 1) - 1),
      }));
      queryClient.setQueryData<NotificacaoResponse[]>(['notificacoes-lista'], (old) =>
        old?.map((n) => (n.id === notificacao.id ? { ...n, lida: true } : n))
      );
      await notificacoesApi.marcarLida(notificacao.id);
    }
  }

  async function handleMarcarTodasLidas() {
    queryClient.setQueryData<{ contagem: number }>(['notificacoes-contagem'], { contagem: 0 });
    queryClient.setQueryData<NotificacaoResponse[]>(['notificacoes-lista'], (old) =>
      old?.map((n) => ({ ...n, lida: true }))
    );
    await notificacoesApi.marcarTodasLidas();
  }

  const notificacoes = listaQuery.data ?? [];

  return (
    <div className="relative" ref={containerRef}>
      <button
        type="button"
        onClick={() => setOpen((v) => !v)}
        aria-label="Notificações"
        aria-expanded={open}
        className={cn(
          'relative flex h-10 w-10 items-center justify-center rounded-lg text-text-secondary',
          'transition-colors duration-200 hover:bg-text-primary/5 hover:text-text-primary',
          'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/30',
        )}
      >
        <NotificacaoIcon />
        {contagem > 0 && (
          <span className="absolute right-1 top-1 flex h-4 min-w-4 items-center justify-center rounded-full bg-danger px-1 text-[10px] font-semibold text-white">
            {contagem > 9 ? '9+' : contagem}
          </span>
        )}
      </button>

      {open && (
        <div className="absolute right-0 z-20 mt-2 w-80 rounded-lg border border-border bg-surface shadow-lg">
          <div className="flex items-center justify-between border-b border-border px-4 py-3">
            <span className="text-sm font-medium text-text-primary">Notificações</span>
            {contagem > 0 && (
              <button
                type="button"
                onClick={() => void handleMarcarTodasLidas()}
                className="text-xs font-medium text-primary hover:underline"
              >
                Marcar todas como lidas
              </button>
            )}
          </div>

          <div className="max-h-96 overflow-auto">
            {listaQuery.isLoading && (
              <p className="px-4 py-6 text-center text-sm text-text-secondary">Carregando...</p>
            )}
            {!listaQuery.isLoading && notificacoes.length === 0 && (
              <p className="px-4 py-6 text-center text-sm text-text-secondary">Nenhuma notificação.</p>
            )}
            <ul>
              {notificacoes.map((notificacao) => (
                <li key={notificacao.id}>
                  <button
                    type="button"
                    onClick={() => void handleItemClick(notificacao)}
                    className={cn(
                      'flex w-full flex-col gap-0.5 border-b border-border px-4 py-3 text-left last:border-0',
                      'transition-colors duration-200 hover:bg-text-primary/5',
                    )}
                  >
                    <div className="flex items-center gap-2">
                      {!notificacao.lida && <span className="h-1.5 w-1.5 shrink-0 rounded-full bg-primary" />}
                      <span className="truncate text-sm font-medium text-text-primary">{notificacao.titulo}</span>
                    </div>
                    <p className="truncate text-xs text-text-secondary">{notificacao.corpo}</p>
                    <span className="font-mono text-[10px] text-text-secondary">
                      {formatRelativeTime(notificacao.createdAt)}
                    </span>
                  </button>
                </li>
              ))}
            </ul>
          </div>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Adicionar o `NotificationBell` no `Header`**

Em `apps/web/src/components/Layout/Header.tsx`, adicionar o import:

```typescript
import { NotificationBell } from '@/components/Layout/NotificationBell';
```

E renderizar entre `<ThemeToggle />` e o bloco `hidden items-center gap-3 md:flex` (dentro da
`<div className="flex items-center gap-2">`):

```tsx
        <div className="flex items-center gap-2">
          <ThemeToggle />
          <NotificationBell />

          <div className="hidden items-center gap-3 md:flex">
```

- [ ] **Step 3: Verificar compilação**

Run: `cd apps/web && npm run typecheck`
Expected: sem erros de tipo.

- [ ] **Step 4: Testar manualmente no navegador**

Rodar `npm run dev` nos dois apps. Logar como morador. Gerar um evento (ex.: pedir que um admin
crie um comunicado, ou usar as contas de teste pra dois papéis em abas diferentes). Verificar:

- Badge aparece no sino com a contagem correta, "9+" acima de 9 (pode simular criando várias
  notificações rápido).
- Clicar no sino abre o dropdown com a lista, fecha ao clicar fora e ao apertar ESC.
- Clicar num item marca como lida (bolinha some, badge decrementa) e navega pra rota certa.
- "Marcar todas como lidas" zera o badge e some as bolinhas da lista aberta.
- Repetir em modo claro e escuro (`ThemeToggle`).
- Esperar ~30s com o dropdown fechado e confirmar que o badge atualiza sozinho após um novo evento
  (polling).

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/Layout/NotificationBell.tsx apps/web/src/components/Layout/Header.tsx
git commit -m "feat(web): sino de notificacoes no header (fatia 4.7)"
```

---

### Task 14: Marcar a fatia como concluída no roadmap

**Files:**
- Modify: `docs/roadmap.md`

- [ ] **Step 1: Marcar a Fatia 4.7 como concluída**

Em `docs/roadmap.md`, no bullet da Fatia 4.7, substituir:

```
- **Fatia 4.7 — Central de notificações in-app.** Sino no header com badge de não lidas, alimentado
  pelos mesmos eventos que já disparam push.
```

por (acrescenta só `✅ Concluída.` ao final, mesmo padrão das fatias 4.1-4.6 acima dela):

```
- **Fatia 4.7 — Central de notificações in-app.** Sino no header com badge de não lidas, alimentado
  pelos mesmos eventos que já disparam push. ✅ Concluída.
```

- [ ] **Step 2: Commit**

```bash
git add docs/roadmap.md
git commit -m "docs: marca fatia 4.7 (central de notificacoes) como concluida"
```
